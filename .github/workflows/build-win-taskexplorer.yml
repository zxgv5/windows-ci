name: build-win-taskexplorer

on:
  workflow_dispatch:
  workflow_call:

env:
  QT_VERSION: '6.8.1'
  VS_VERSION: '2022'
  QT_ARCH: 'win64_msvc2022_64'
  QT_BIN_SUFFIX: 'msvc2022_64'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout source repo
        uses: actions/checkout@main
        with:
          repository: DavidXanatos/TaskExplorer
          ref: master
          path: taskexplorer_source
          fetch-depth: 0
          submodules: recursive

      - name: Checkout ci source repo
        uses: actions/checkout@main
        with:
          path: ci_source

      - name: Generate timestamp
        shell: bash
        run: |
          echo "BUILD_DATE=$(TZ=UTC-8 date +"%y.%m.%d-%H.%M.%S")" >> $GITHUB_ENV

      - name: Install qt
        shell: pwsh
        run: |
          pip install aqtinstall
          # 安装 Qt 库（qtcharts 在 Qt6 中仍存在，qtscript 已废弃，改用 qt5compat 提供兼容）
          aqt install-qt --outputdir "${{ github.workspace }}\taskexplorer_source\Qt" `
            windows desktop "${{ env.QT_VERSION }}" "${{ env.QT_ARCH }}" `
            --modules qtcharts qt5compat
          # 安装 Qt Creator 工具（内含 jom.exe）
          aqt install-tool --outputdir "${{ github.workspace }}\taskexplorer_source\Qt" `
            windows desktop tools_qtcreator
          # 将 Qt bin 目录加入 PATH
          echo "${{ github.workspace }}\taskexplorer_source\Qt\${{ env.QT_VERSION }}\${{ env.QT_BIN_SUFFIX }}\bin" `
            | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Patch buildVariables.cmd
        shell: pwsh
        working-directory: ./taskexplorer_source/Build
        run: |
          $file = "buildVariables.cmd"
          (Get-Content $file) -replace 'set "qt_version=5\.15\.2"', 'set "qt_version=6.8.1"' | Set-Content $file
          Write-Host "Patched $file to use qt_version=6.8.1"

      - name: Patch prepTools.cmd
        shell: pwsh
        working-directory: ./taskexplorer_source/Build
        run: |
          $file = "prepTools.cmd"
          (Get-Content $file) -replace 'set qt_build=msvc2019_64', 'set qt_build=msvc2022_64' | Set-Content $file
          Write-Host "Patched $file to use qt_build=msvc2022_64"

      - name: Patch all scripts
        shell: pwsh
        working-directory: ./taskexplorer_source
        run: |
          $oldString = 'msvc2019_64'
          $newString = 'msvc2022_64'
          Get-ChildItem -Recurse -Include *.cmd,*.pro,*.pri,*.conf | ForEach-Object {
            (Get-Content $_.FullName) -replace $oldString, $newString | Set-Content $_.FullName
          }
          Write-Host "Patched all scripts to use $newString"

      - name: Build app
        shell: cmd
        working-directory: ./taskexplorer_source/Build
        run: |
          call "C:\Program Files\Microsoft Visual Studio\${{ env.VS_VERSION }}\Enterprise\VC\Auxiliary\Build\vcvarsall.bat" x64
          buildRelease.cmd x64

      - name: Create zip archive locally
        working-directory: ./taskexplorer_source
        shell: pwsh
        run: |
          $zipName = "../dist/taskexplorer-${{ env.BUILD_DATE }}.zip"
          $source = "./bin/x64/Release"
          $tempDir = "./temp_zip_dir"
          New-Item -ItemType Directory -Path "../dist" -Force | Out-Null
          if (Test-Path $tempDir) { Remove-Item -Recurse -Force $tempDir }
          New-Item -ItemType Directory -Path $tempDir -Force | Out-Null
          robocopy $source $tempDir * /S
          if ($LASTEXITCODE -ge 8) {
              throw "robocopy failed with exit code $LASTEXITCODE"
          }
          Compress-Archive -Path "$tempDir/*" -DestinationPath $zipName -CompressionLevel Optimal -Force
          Remove-Item -Recurse -Force $tempDir
          Write-Host "zip archive created: $zipName"
          exit 0

      - name: Encrypt package and create release
        uses: ./ci_source/.github/actions/aes256pac_release
        with:
          ZIP_AES256_PWD: ${{ secrets.ZIP_AES256_PWD }}
          BUILD_ARTIFACT_PATH: "./dist/taskexplorer-${{ env.BUILD_DATE }}.zip"
          RELEASE_TAG_NAME: "${{ env.BUILD_DATE }}-taskexplorer"